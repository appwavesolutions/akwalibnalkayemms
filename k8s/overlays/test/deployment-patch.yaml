apiVersion: apps/v1
kind: Deployment
metadata:
  name: akwalibnalkayemms
spec:
  template:
    spec:
      containers:
      - name: akwalibnalkayemms
        resources:
          requests:
            memory: "256Mi"
            cpu: "200m"
          limits:
            memory: "512Mi"
            cpu: "400m"
